package rw.bk.filmfan.helpers;import java.text.SimpleDateFormat;import java.util.Date;public class dateHelpers {    public static String convertdate(String release_year){        String[] splitDate = release_year.split("-", 4);      return splitDate[0];    }}