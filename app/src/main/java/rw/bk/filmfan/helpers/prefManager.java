package rw.bk.filmfan.helpers;import android.content.SharedPreferences;import org.json.JSONException;import org.json.JSONObject;public class prefManager {    // check if user has logged in    public static boolean checkUser(SharedPreferences sharedPreferences){        boolean isAuthenticated=sharedPreferences.getBoolean("isAuthenticated",false);        return isAuthenticated;    }    // store user session    public static boolean storeUserSession(SharedPreferences sharedPreferences,String userSession){        SharedPreferences.Editor editor = sharedPreferences.edit();        editor.putBoolean("isAuthenticated", true);        editor.putString("userSession",userSession);        editor.commit();        return true;    }    // get user session    public static String getUserSession(SharedPreferences sharedPreferences){        String session_id="";            session_id=sharedPreferences.getString("userSession","");        return session_id;    }}