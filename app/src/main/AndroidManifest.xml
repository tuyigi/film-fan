<?xml version="1.0" encoding="utf-8"?><manifest xmlns:android="http://schemas.android.com/apk/res/android"    package="rw.bk.filmfan">    <application        android:allowBackup="true"        android:icon="@mipmap/ic_launcher"        android:label="@string/app_name"        android:roundIcon="@mipmap/ic_launcher_round"        android:supportsRtl="true"        android:theme="@style/Theme.FilmFan">        <activity android:name=".activities.favorite_movie.favorite"></activity>        <activity android:name=".activities.movie_details.details" /> <!-- <activity android:name=".activities.home.home"></activity> -->        <activity android:name=".activities.login.login" />        <activity android:name=".activities.home.home">            <intent-filter>                <action android:name="android.intent.action.MAIN" />                <category android:name="android.intent.category.LAUNCHER" />            </intent-filter>        </activity>    </application>    <uses-permission android:name="android.permission.INTERNET" /></manifest>